[project]
name = "remote-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Guruk26", email = "gururajkhule26@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastmcp>=4.0.5",
]

[project.scripts]
remote-server = "remote_server:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
